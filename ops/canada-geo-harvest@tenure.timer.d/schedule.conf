[Timer]
# OnCalendar= with no value RESETS the inherited list. Without it systemd
# ADDS to the template schedule, so this timer would keep firing daily as well.
OnCalendar=
# Tenure is the business-critical daily loop (PLAN_C3 3.1).
OnCalendar=*-*-* 05:30:00
