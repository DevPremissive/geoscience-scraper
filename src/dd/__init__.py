"""dd — target-time due diligence over assessment reports (C5.1).

`ingest` builds a per-target corpus from the PDFs C3.4 fetched; `rag` answers the
fixed question set over it with citations. Both are per-target by design: dozens
of reports for one deal, never a province (that is C5.3).
"""
